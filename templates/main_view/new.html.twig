{% extends 'base.html.twig' %}

{% block title %}
  New MainView
{% endblock %}

{% block body %}
  <div class="container mt-5">
    <h1>Create New MainView</h1>

    <div class="card">
      <div class="card-body">{{ include('main_view/_form.html.twig', { button_label: 'Create' }) }}</div>
    </div>

    <div class="mt-3">
      <a href="{{ path('app_main_view_index') }}" class="btn btn-secondary">Back to List</a>
    </div>
  </div>
{% endblock %}
