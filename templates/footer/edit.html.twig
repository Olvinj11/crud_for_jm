{% extends 'base.html.twig' %}

{% block title %}
  Edit Footer
{% endblock %}

{% block body %}
  <div class="container mt-4">
    <h1 class="mb-4">Edit Footer</h1>

    {{ include('footer/_form.html.twig', { button_label: 'Update' }) }}

    <div class="mt-3">
      <a class="btn btn-secondary" href="{{ path('app_footer_index') }}">Back to List</a>
    </div>

    {{ include('footer/_delete_form.html.twig') }}
  </div>
{% endblock %}
