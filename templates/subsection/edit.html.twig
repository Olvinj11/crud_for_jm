{% extends 'base.html.twig' %}

{% block title %}
  Edit Subsection
{% endblock %}

{% block body %}
  <div class="container mt-4">
    <h1 class="mb-4">Edit Subsection</h1>

    {{ include('subsection/_form.html.twig', { button_label: 'Update' }) }}

    <div class="mt-3">
      <a href="{{ path('app_subsection_index') }}" class="btn btn-primary">Back to List</a>
    </div>

    <div class="mt-3">{{ include('subsection/_delete_form.html.twig') }}</div>
  </div>
{% endblock %}
