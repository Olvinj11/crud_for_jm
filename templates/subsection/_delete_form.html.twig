<form method="post" action="{{ path('app_subsection_delete', { id: subsection.id }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-3">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subsection.id) }}" />
  <button type="submit" class="btn btn-danger">Delete</button>
</form>
