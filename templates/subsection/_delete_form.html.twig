<form method="post" action="{{ path('app_subsection_delete', {'id': subsection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subsection.id) }}">
    <button class="btn">Delete</button>
</form>
